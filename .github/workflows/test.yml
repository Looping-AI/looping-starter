name: Test

# Both lines need the suite, or a PR into `next` merges unchecked. What each
# branch installs is AGENTS.md's to say, not this file's.
on:
  push:
    branches:
      - main
      - next
  pull_request:
    branches:
      - main
      - next

jobs:
  test:
    timeout-minutes: 15
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - uses: actions/setup-node@v6
        with:
          node-version: 24

      # From the committed lockfile, never from a sibling checkout — whether
      # that lockfile resolves to the registry or to a git ref is the branch's
      # business. `npm run link:local` is a development convenience, and a build
      # that only works with it must never be able to ship.
      - run: npm ci

      - run: npm run check
      - run: npm test

      # Each agent's module graph carries only the plugins that agent installs,
      # and stays under its size ceiling. The one defence that survives a
      # refactor: without it the subpath-export discipline in @dynamicagents/plugins
      # rots silently the first time someone adds a convenience re-export.
      - run: npm run verify:isolation

      # The whole Worker builds and every binding resolves.
      - run: npx wrangler deploy --dry-run --outdir dist
