# looping-starter

Starter repo for building a Looping agent on Cloudflare Workers, on top of @looping/core
and @looping/plugins. Ships three working examples — a reactive delegating agent, a
proactive triage agent, and an ARC-AGI player. Grow the one you want and delete the rest;
adding or removing a capability is a single line in src/plugins.ts.
